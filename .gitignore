# graphify knowledge-graph output — regenerated by `graphify build`, ~900 kB per run
graphify-out/

# Spendly's own exports. These contain real financial records; keeping them out of
# version control is the point, not tidiness.
spendly-backup.json
spendly-export.csv
